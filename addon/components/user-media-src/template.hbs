{{did-update (perform this._restartStream) @mediaConstraints}}
{{#if (has-block)}}
  {{yield (hash this.videoUrl this.videoStream)}}
{{else}}
  {{!-- template-lint-disable require-media-caption --}}
  <video autoplay="true" srcObject={{this.videoStream}}/>
{{/if}}
